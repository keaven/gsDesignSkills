---
name: simtrial
description: >
  Guide users through clinical trial simulation using the simtrial R package.
  Use this skill when the user asks about: simulating survival trials, simfix,
  sim_pw_surv, cutting data at calendar or event times, weighted logrank tests,
  MaxCombo tests, or simulation-based power.
---

# Clinical Trial Simulation with simtrial

TODO: Add skill content covering:
- `sim_pw_surv()` for piecewise survival simulation
- `sim_fixed_n()` for fixed-sample simulation
- `cut_data_by_date()` and `cut_data_by_event()` for data cuts
- `wlr()` for weighted logrank tests
- `maxcombo()` for MaxCombo tests
- `counting_process()` for at-risk/event tables
- Simulation workflows for power estimation
- Non-proportional hazards simulation (delayed effect, crossing)
